name: ci
on:
  push: { branches: [main, "release/**"] }
  pull_request:
  merge_group:
permissions:
  contents: read   # the suite only reads the checkout
jobs:
  suite:
    uses: public-software/.github/.github/workflows/rust.yml@0143b99c390360ed1fd66e6c266ebbd74ad7d4cf # v2.0.0
