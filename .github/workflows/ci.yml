name: ci
on:
  push: { branches: [main, "release/**"] }
  pull_request:
  merge_group:
permissions:
  contents: read   # the suite only reads the checkout
jobs:
  suite:
    uses: public-software/.github/.github/workflows/rust.yml@722dce0ec016d1c572269789a89e989199b24e1d # v1.1.1
